{{!< layout}}
<div id="gh-unlock_signup" class="gh-unlock_signup level-item">

  <div class="gh-unlock_signup-content ">
    <div class="box p-6">
      <h1 class="is-size-1 mb-6 has-text-centered">Ghost Unlock</h1>
        <h2 class="subtitle  mb-5 has-text-centered">Link email address complete sign up</h2>
        {{#if (hasValidKey hasKey)}}
          <p>Its {{hasKey}} this {{address}} has a valid key for the Unlock Protocol lock.</p>
        {{else}}
          <p>Its {{hasKey}} this {{address}} does not have a valid key for the Unlock Protocol lock.</p>
        {{/if}}
        <form method="post" class="gh-unlock_signup-form" action="/signup/verify">
          <input class="input mt-1 mb-5" type="email" id="email" name="email" placeholder="email@example.com">
          <button type="submit" class="button is-success is-fullwidth gh-unlock_signup-btn mb-5 ">Sign up</button>
        </form>
    </div>
  </div>
  <div id="gh-unlock_signup-modal" class="modal">
    <div class="modal-background"></div>

    <div class="modal-content ">
      <div class="box p-6 has-text-centered">
        <i class="is-size-1 fa-regular fa-envelope mb-4"></i>
        <h3 class="is-size-3 mb-5 has-text-weight-bold">Now check your email!</h3>
        <p class="mb-4">To complete signup, click the confirmation link in your inbox, if it doesn't arrive within 3 minutes, check your spam folder</p>
      </div>
    </div>
  </div>

</div>